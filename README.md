# ak-finccam/r-packages

Temporary R package repository for finccam fork binaries.

Install in R:
`
install.packages('Rblpapi', repos = c('https://ak-finccam.github.io/r-packages', 'https://cloud.r-project.org'))
`
